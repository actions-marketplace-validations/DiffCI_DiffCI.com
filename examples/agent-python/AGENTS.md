# DiffCI Agent Instructions

Before marking changes PR-ready, run:

```bash
npx "@diffci.com/diffci@latest" check
```

Use DiffCI output as a CI/CD validation lens. Keep normal Python validation commands authoritative.
