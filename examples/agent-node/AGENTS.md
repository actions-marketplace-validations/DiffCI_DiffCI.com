# DiffCI Agent Instructions

Before marking changes PR-ready, run:

```bash
npx "@diffci.com/diffci@latest" check
```

DiffCI is observation-only by default. Use its report to choose focused validation, but keep required
project CI authoritative.
